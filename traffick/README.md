# __MODULE_TITLE__ Media

Public promotional media for **__MODULE_TITLE__**.

- Module ID: `__MODULE_ID__`
- `gifs/`: animated feature demonstrations.
- `images/`: screenshots, banners, and other still images.

## Media links

Add one row per asset after uploading it. The row below is an example; replace it with a real filename and description.

| Asset | Description / alt text | Direct image URL |
| --- | --- | --- |
| `__MODULE_ID__-feature-demo.gif` | Describe the feature shown in this GIF. | `https://raw.githubusercontent.com/GnollStack/Bakshi-s-Bazaar-Media/main/__MODULE_ID__/gifs/__MODULE_ID__-feature-demo.gif` |

For still images, use the same URL structure with `images/` and the actual filename.

## Publishing

1. Add the files to `gifs/` or `images/`.
2. Replace the example row above with the actual filenames, useful alt text, and direct URLs.
3. Add this module to the repository's main README.
4. Commit and push, then check that each URL works without signing in.
5. Paste the direct URLs into your Foundry description's image Source fields or HTML.

Keep paths stable after publishing so existing store-page images keep loading.
